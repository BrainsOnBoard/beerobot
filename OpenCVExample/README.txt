To get this code to compile, you need to set the OPENCV_DIR environment variable to wherever your OpenCV is downloaded to, e.g.:
	OPENCV_DIR=C:\opencv\build

Note the \build on the end!

Use the OpenCVExample configuration, rather than Debug or Release.